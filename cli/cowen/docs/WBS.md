# cli/cowen v0.3.3 全栈执行级 WBS (Master Blueprint)

## Phase 1: 内部治理与架构收敛 (Internal Governance & Convergence) - [COMPLETED]
**核心目标**：通过架构重构与存储层归一化，将系统内部复杂度降低 30%，并实现配置交互的完全自治。

| ID | 任务名称 | 状态 | 关键产物 | 估时 (MD) | 依赖 | 质量门禁 (Quality Gates) |
| :--- | :--- | :--- | :--- | :---: | :---: | :--- |
| **1.1** | **ProfileWorker 状态机** | [x] | `state.rs`, `manager.rs` 重构 | 2.5 | - | 1. [TDD] 9 条状态转移边覆盖 100%。<br>2. [重构] 移除所有显式的 `drop(lock)`。 |
| **1.2** | **Config 数组路径算子** | [x] | 增强版 `path_parser.rs` | 1.5 | - | 1. [验证] 支持 `a.key:val.b` 及 `+` 语法。<br>2. [E2E] 通过 `unset` 验证数组坍缩。 |
| **1.3** | **FileStore 物理拆分** | [x] | `file.rs`, `migration.rs` | 1.5 | 1.4 | 1. [重构] 核心方法嵌套深度 ≤ 3 层。<br>2. [验证] 旧版数据自动在线迁移成功。 |
| **1.4** | **模型归一化** | [x] | `store_trait.rs` | 1.0 | - | 1. [重构] 存储模板代码减少 40%。<br>2. [验证] 加解密一致性。 |
| **1.5** | **全量回归验证** | [x] | 测试报告, 新测例 | 1.0 | 1.1-1.4 | 1. [验证] Case 01-54 全量通过。<br>2. [验证] 模拟 5 分钟内 5 次 Panic 触发熔断。 |

## Phase 2: 系统健壮性与可维护性专项 (Robustness & Maintainability) - [PLANNED]
**核心目标**：消除硬编码逻辑，实现 100% 容器化测试闭环，降低工程维护成本。

| ID | 任务名称 | 状态 | 目标与方法 | 关键产物 | 估时 (MD) | 依赖 | 质量门禁 |
| :--- | :--- | :--- | :--- | :--- | :---: | :---: | :--- |
| **2.1** | **ConfigManager 彻底扁平化** | [ ] | **方法**: 引入策略模式，根据 `StorageConfig` 类型动态分发。 | `ConfigStrategy` SPI | 1.5 | 1.2 | 1. [重构] 消除 `config_manager.rs` 中 100+ 行 `match` 硬编码。 |
| **2.2** | **容器化测试闭环** | [ ] | **方法**: 将所有 DB 依赖及测试脚本封装至独立容器环境。 | `Dockerfile.test` | 1.0 | 1.5 | 1. [验证] 在无 Homebrew 环境的 CI 中一键运行全量测试。 |
| **2.3** | **Makefile 模块化拆解** | [ ] | **方法**: 将构建、容器、环境准备等逻辑拆分为独立脚本。 | `scripts/build/*.sh` | 0.5 | - | 1. [维护] `Makefile` 主文件行数减少 60%。 |
| **2.4** | **存储碎片检测 (GC) 集成** | [ ] | **方法**: 将 LLD 定义的 `list_orphans` 集成至 `cowen doctor`。 | `doctor.rs` 增强 | 0.5 | 1.3 | 1. [验证] 成功识别并提示清理孤儿文件。 |

**当前总进度**: 100% (Phase 1)
**下一步计划**: Phase 2 启动
