# cowen-wasm-facade Design Notes

本文档用于记录该模块在开发过程中的架构设计考量、选型决策记录 (ADR) 以及任何特殊的历史包袱说明。

## 设计决策 (Architecture Decisions)
- **Wasmtime 宿主隔离**：因为我们需要在运行时动态更新鉴权逻辑等核心模块，故选择了 WebAssembly 作为沙盒环境。
- **内存安全边界**：所有从 Wasm 模块内部读取数据的操作必须受到长度限制，以防范恶意的 Wasm 代码通过构造过长的字符串或缓冲区攻击主进程。

## 时序流或关系图
*(暂无时序流图表)*
