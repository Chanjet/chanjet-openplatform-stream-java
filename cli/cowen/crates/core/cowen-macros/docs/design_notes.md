# cowen-macros Design Notes

本文档用于记录该模块在开发过程中的架构设计考量、选型决策记录 (ADR) 以及任何特殊的历史包袱说明。

## 设计决策 (Architecture Decisions)
- **抽象的代价转移至编译期**：通过派生宏生成模板样板代码，提高开发效率，而不牺牲运行期的性能开销。这也要求其依赖必须最简化（只有 `syn` 和 `quote`）。

## 时序流或关系图
*(暂无时序流图表)*
