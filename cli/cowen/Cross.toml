[target.x86_64-unknown-linux-gnu]
pre-build = [
    "dpkg --add-architecture amd64 || true",
    "apt-get update && apt-get install --assume-yes pkg-config libssl-dev ca-certificates xz-utils curl",
    "mkdir -p /.cache && chmod 777 /.cache"
]
