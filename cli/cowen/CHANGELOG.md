# Cowen CLI 更新日志 (Changelog)

所有针对 `cowen` 命令行工具的功能变更都将记录在本文档中。

---

## [0.3.6] - 2026-05-29

### 🚀 新特性 (Features)
- **守护进程原生自启与物理脱壳 (Daemon Native Auto-start & Decoupling)**:
  - 彻底解除了 `cowen` CLI 在开机启动阶段的套壳代理行为。系统服务（launchd / systemd / sc）现在直接拉起底层的 `cowen-daemon` 二进制执行体。
  - `cowen-daemon` 原生支持 `--auto-start-all` 参数，可独立完成环境配置加载与 IPC 调度，实现真正的单进程后台驻留，大幅降低系统资源开销。
- **Windows 环境静默启动 (Windows Silent Boot)**:
  - 为 Windows 平台的守护进程引入了 `windows_subsystem = "windows"` 编译属性。现在 Windows 系统开机自启后台服务时将完全静默运行，彻底消除了开机时残留命令行黑窗（Console Window）闪烁的问题。

---

## [0.3.5] - 2026-05-22

### 🚀 新特性 (Features)
- **全局配置分层重构与彻底隔离 (Global Config Migration)**:
  - **分层寻址**: 成功将 `security`、`log`、`openapi_url`、`stream_url` 和 `search` 等公共基础设施配置移入全局 `app.yaml`。敏感业务数据（如 `app_secret`、`encrypt_key`）依然物理隔离在各自的 Profile 层。
  - **强制隔离边界**: Profile 加载器强制执行隔离校验，在 Profile 中严禁定义任何已被上移至全局的配置项，从根本上杜绝了多环境的合并冲突。
  - **平滑无感迁移**: 首次启动时自动静默触发平滑迁移，以当前激活的 Current Profile 配置为基准自动合并至全局 `app.yaml`，并物理清除各 Profile 中的冗余字段。
- **编译参数注入与强校验拦截 (Build-time Injection)**:
  - 移除了代码中硬编码的 `BUILTIN_CLIENT_ID` 等默认值。通过 `build.rs` 捕获环境变量并在编译期使用 `env!()` 固化。
  - 引入强校验机制，若未在 Makefile / 编译命令行中提供必需的 `COWEN_BUILD_*` 环境变量，编译将直接触发 `panic!` 报错退出。
  - 注入的编译元数据支持在 `cowen version --debug` 命令中统一公开展示，以提升运维诊断透明度。
- **插件化系统重置与 Dry Run (OCP Modular Reset)**:
  - 定义了 `Resettable` Trait 并结合 `inventory` 静态注册收集器（OCP），将 `reset` 命令重构为通用的两相调度器，新增任何状态化存储组件均可自动被收集参与重置。
  - 支持 `--dry-run` 参数，可在执行正式擦除前安全地在控制台展示拟删除的物理文件、数据库表清单而不产生任何副作用。
- **IPC / UDS 路径防溢出 (IPC Path Robustness)**:
  - 针对自定义长工作区路径导致的操作系统 UDS 长度超限限制，引入 SHA-256 唯一哈希缩短机制，自动将套接字文件安全映射到 `/tmp/cowen_<HASH>.sock`，解决极端环境下的 IPC 绑定失败问题。

### 🔧 改进与修复 (Improvements & Fixes)
- **E2E 失败断言优雅标准化**: 将 52 个测试脚本中的 152 处非标准 inline `exit 1` 统一升级为规范的 `fail_suite` 标准断言 API，消除了测试控制流中的冗余与偶发性挂起，大幅度提高了 E2E 跑测的表达力。

---

## [0.3.3] - 2026-05-21

### 🚀 新特性 (Features)
- **Worker 生命周期状态机 (Worker State Machine)**: 
  - 引入了 `ProfileWorker` 手写异步状态机，收拢控制流生命周期状态。
  - 配套实现了基于指数退避算法的重试（最大退避 60s）和熔断机制（5 分钟内失败超 5 次则熔断）。处于 `Failed` 熔断状态的进程必须使用手写指令显式 `cowen daemon restart` 恢复，避免了后台无效自启震荡引发的 CPU/IO 爆仓。
- **配置下标自治与物理坍缩 (Enhanced Config Engine)**:
  - 实现了 `array.key:value.field` 的键值寻址及即时绑定机制。
  - 支持在 `unset` 元素后对索引进行自动物理坍缩重排，向用户彻底屏蔽了数组下标的管理复杂度。
- **存储层物理标准化与平滑迁移 (Storage Migration)**:
  - 规范存储目录拓扑为 `vault/{profile}/{prefix}/{id}.json`，开机自启静默迁移 v2 数据资产，并提供孤儿文件 GC 检测接口。

---

## [0.3.2] - 2026-05-21

### 🚀 新特性 (Features)
- **优雅关机与单守护进程稳定性 (Graceful Shutdown & Stability)**:
  - 重构了整个 Daemon 的并发调度，对多 Profile 共享底座进行架构稳定性加固。
  - 强化了后台线程的信号监听与优雅关机（Graceful Shutdown）物理拦截，在强行退出时能够干净回收连接，绝不遗留悬挂僵尸进程或发生端口冲突。

---

## [0.3.1] - 2026-05-19

### 🚀 新特性 (Features)
- **可插拔搜索架构 (Pluggable Search Architecture)**: 核心引擎与复杂的语义搜索能力实现物理隔离。支持通过动态链接库 (`.so/.dylib`) 扩展搜索算法，在保持主二进制程序轻量化的同时，赋予了系统极强的扩展性。
- **智能自适应刷新 (Adaptive Token Refresh)**: 摒弃了硬编码的定时轮询，引入了基于 Token 剩余寿命 (80% 规则) 的自适应计算算法，并配套随机抖动 (Jitter)，有效防止大规模部署时的“惊群效应”与服务端冲击。
- **一键环境诊断工具 (cowen doctor)**: 新增深度体检指令，可实时检测网络延迟、存储权限、插件版本一致性及系统关键组件的运行健康度。

### 🔧 改进与修复 (Improvements & Fixes)
- **企业级死锁防护机制**: 
    - **SQLite 并发加固**: 优化连接池策略 (`max_connections=5`) 并强制 WAL 模式，彻底解决了多 Profile 并发重命名或写入时的高 CPU 夯死问题。
    - **异步锁架构重构**: 在 OAuth2 场景下，将传统的同步阻塞文件锁重构为异步友好的 `try_lock` 轮询模式，确保了在高并发压力下 Tokio 线程池的持续响应能力。
- **自愈鲁棒性增强**: 
    - **DLQ 初始化防护**: 重构了死信队列 (DLQ) 的启动流程，移除了潜在的 Panic 点，实现了存储故障时的优雅降级与清晰的错误反馈。
    - **退出码标准化**: 修复了 Daemon 启动失败时可能产生的静默退出问题，确保 CLI 在初始化异常时始终返回非零退出码。
- **物理模块隔离 (Crate isolation)**: 完成了全工程的物理解耦，提升了编译速度与代码的可维护性，为未来的 SDK 独立分发奠定了基础。

---

## [0.3.0] - 2026-04-30

### 🚀 新特性 (Features)
- **Store App (应用商店模式)**: 专为 ISV 量身定制的多租户侧车 (Sidecar) 方案。支持大规模租户的身份托管、令牌自动续约以及消息统一汇聚转发，极大简化了 SaaS 应用的集成难度。
- **内置数据库默认化**: 新用户默认启用内置 SQLite 存储，支持原生的消息审计与死信队列管理，无需额外配置。
- **纯云端 Redis 存储支持**: 支持将 Redis 设为主持久化存储，实现在无磁盘环境（如 Serverless/Container）下的零文件运行。
- **多维存储与缓存模式 (Storage & Cache Modes)**: 全面支持存储层与缓存层的自由组合。用户可根据规模需求，在单机加密文件、外置关系型数据库（MySQL/Postgres）以及分布式缓存（Redis）之间灵活切换，实现从开发到生产的平滑过渡。
- **云原生 Sidecar 深度优化**:
    - **环境变量一键启动**: 实现了 `COWEN_*` 全局环境变量注入，支持通过容器环境变量直接定义 AppKey、AppSecret、存储底座等核心参数。
    - **隐式自愈初始化**: 优化了 `daemon start` 逻辑，当检测到未初始化但环境凭据齐全时，自动触发静默初始化并拉起网关。
    - **分布式一致性保障**: 在 Redis 存储模式下引入 Lua 原子 CAS 操作，保障集群模式下多 Pod 同时启动时的 Token 交换一致性。
- **一键数据迁移工具**: 新增 `cowen store migrate` 指令，支持在上述所有存储模式之间进行全量数据平滑搬迁。

### 📄 文档优化
- **全场景接入手册**: 更新了针对自建、商店应用及 OAuth2 的典型存储场景配置指南。


## [0.2.1] - 2026-04-27

### 🚀 新特性 (Features)
- **OAuth2 会话生命周期自清理**: 实现了认证流程的自动化清理机制。当认证超时、失败或重新发起登录时，系统将自动清除本地残留的中间状态，确保状态机始终处于纯净状态。
- **Webhook 回环地址强制限制**: 强化了安全性，强制所有本地监听服务（Proxy 与 OAuth2 Callback）必须绑定在回环地址 (`127.0.0.1` 或 `::1`)。任何尝试绑定公网或局域网 IP 的配置都将被安全拦截。

### 🔧 改进与修复 (Improvements & Fixes)
- **Windows 守护进程挂死自愈**: 解决了在 Windows 系统更新或休眠恢复后进程可能处于“挂死”状态的问题。引入了 PID + Port 的双重健康检查，发现无响应进程时会自动执行强制重启。
- **OAuth2 交互流程优化**: 移除了 `cowen init` 流程中由于无法跨端回调而失效的二维码展示，并优化了授权引导文案，明确提示用户需在本机浏览器中完成授权。
- **错误处理鲁棒性增强**: 提升了 OAuth2 监听器启动失败时的异常捕获与反馈质量。

---

## [0.2.0] - 2026-04-21

### 🚀 新特性 (Features)
- **全自动身份验证支持**: 引入了更便捷的 OAuth2 授权机制，通过自动化流程处理令牌获取，显著降低了初次配置的复杂度。
- **守护进程自启动支持**: 新增 `cowen daemon service` 命令，支持在 macOS、Linux 及 Windows 上一键配置守护进程的开机自启动。
- **安装过程自动激活服务**: 优化了全平台安装体验，在安装 Cowen CLI 时将自动完成自启动服务的配置与激活，实现即装即用。
- **Profile 智能管理体系**:
    - **环境一键迁移**: 新增 `cowen profile rename` 命令，支持配置环境及其凭据的完整迁移。
    - **自动初始化优化**: 支持自动化的环境命名机制，并强化了保护措施，防止误覆盖现有配置。
- **轻量化守护进程**: 重构了后台服务逻辑，通过组件拆分提升了运行效率，支持更灵活的按需启动模式。
- **全平台补全增强**: 扩展了对 PowerShell 的原生支持，并优化了命令行补全的自动安装体验。

### 🔧 改进与修复 (Improvements & Fixes)
- **初始化稳定性加固**: 优化了配置保存机制，彻底解决了特定场景下配置文件可能丢失的问题。
- **状态诊断增强**: 升级了 `cowen status` 报告系统，提供更直观、更清晰的多级状态反馈。
- **配置发现机制优化**: 优化了扫描算法，确保在各种环境下都能准确识别并加载已有的配置。
- **诊断系统鲁棒性升级**: `status` 指令集成了多级错误感知能力，能精确捕获采集异常并提供详尽的修复指导。
- **API 交互能力修复**: 解决了在特定模式下获取接口列表与规约时的权限校验拦截问题。
- **发布质量保障**: 引入了更严格的发布验证机制，确保全平台交付产物的可靠性与功能完整性。

---

## [0.1.5] - 2026-04-11

### 🚀 新特性 (Features)
- **动态服务规约同步**: 引入了自动化的接口定义同步机制，确保本地 API 列表与平台能力实时对齐，支持在复杂的网络环境下自动降级与恢复。
- **推送频率退避控制**: 实现了智能的重试退避算法，在遭遇云端流量控制时能自动调整压力，保障系统的长期运行稳定性。
- **隐私与功能开关**: 新增全局参数，支持用户根据需求显式关闭匿名遥测与本地语义搜索功能。
- **全局环境批量管理**: 支持通过单一指令查看或管理所有已配置环境的运行状态。

### 🔧 改进与修复 (Improvements & Fixes)
- **后台服务自愈能力加固**: 显著提升了守护进程的稳定性，确保在系统意外崩溃或异常重启后，能以前所未有的可靠性重新上线。
- **凭证获取体验优化**: 解决了在特定竞态条件下出现的凭据获取延迟问题，大幅缩短了从初始化到服务就绪的等待时间。
- **配置校验预检机制**: 增加了启动时的强制逻辑校验，能即时识别凭据配置错误并提供修正建议，避免无效的后台运行。
- **安装包命名标准化**: 统一并规范了全平台的构建产物命名，集成了更严密的产物完整性校验流程。
- **日志与安全脱敏**: 进一步强化了全链路日志的敏感数据自动掩码功能，确保调试信息不泄露。
- **环境隔离加固**: 强化了多实例并存时的资源清理与数据隔离逻辑。

---

## [0.1.4] - 2026-04-09

### 🚀 新特性 (Features)
- **版本溯源增强**: 命令行版本信息中集成了源码版本指纹。
- **多平台性能对齐**: 发布了针对主流 CPU 架构的优化版本，内置了更高效的本地推理引擎。

### 🔧 改进与修复 (Improvements & Fixes)
- **接口列表获取修复**: 解决了在大规模 API 授权场景下的列表展示不全问题。
- **运行稳定性增强**: 提升了断线后的秒级自愈能力。

---

## [0.1.3] - 2026-04-07

### 🚀 新特性 (Features)
- **安装辅助工具**: 引入了原生的图形化安装程序。
- **管理看板增强**: 刷新规约时支持更直观的接口统计。

### 🔧 改进与修复 (Improvements & Fixes)
- **交互体验优化**: 支持更直观的分页查询。
- **运行状态修复**: 解决了在特定临界状态下可能出现的进程重启循环。

---

## ⚠️ 已知问题 (Known Issues)

- 目前暂无严重已知阻塞问题。若发现异常，请通过 `cowen status` 获取诊断信息。

---

## 🔮 未来演进与改进 (Future Iterations & Improvements)

- **多环境并行网关支持**: 计划支持在单一机器上同时运行多个 Profile 的网关实例，并提供自动冲突检测与端口管理。
- **本地语义搜索缓存优化**: 提升大规模数据集下的索引加载速度。
