spring:
  application:
    name: open-streaming-gateway
  threads:
    virtual:
      enabled: true # 全局开启 Java 21 虚拟线程支持
  profiles:
    active: localhost # 默认激活本地环境

connector:
  internal-tokens: 
    - cjt-default-internal-token
  internal-token: cjt-default-internal-token # 向后兼容

management:
  server:
    port: 8081 # 独立管理端口，实现管理流与业务流隔离
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,env
  endpoint:
    health:
      show-details: always

server:
  port: 8080
  tomcat:
    threads:
      max: 200 # 开启虚拟线程后，平台线程仅需少量
