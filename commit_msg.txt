【SYKFPT-1093】

补充 E2E 自动化测试用例与跨平台安全修复

1. Phase 1 (AI Engine & Auth Orchestrator):
   - 补充 `e2e_search_engine.rs`，验证 AI Engine JSON-RPC 生命周期。
   - 补充 `test_orchestrator.rs`，验证 Auth Orchestrator 后台任务自动换新和自愈机制。
2. Phase 2 (WASM Sandbox & Config Boundaries):
   - 补充 `test_wasm_sandbox.rs`，测试未授权的 WASM 插件访问被 Sandbox 拦截的行为。
   - 补充 `test_get_value_boundary.rs`，测试配置管理器各类边界条件读写行为。
3. 其他修复:
   - 修复 `inference.rs` 中使用 `#[cfg(target_os)]` 带来的跨平台宏泄露问题，替换为动态判别。
   - 提升整体自动化覆盖率门禁指标。
