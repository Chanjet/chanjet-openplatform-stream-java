chanjet:
  app-key: ${APP_KEY:your_app_key}
  app-secret: ${APP_SECRET:your_app_secret_32_chars}
  gateway-url: ${GATEWAY_URL:wss://open.chanjet.com/gateway}

logging:
  level:
    com.chanjet.connector: INFO
    org.springframework: INFO
